# Dependencias del transcriptor. Instala con:
#   python -m venv .venv
#   .\.venv\Scripts\pip install -r requirements.txt
#
# Ademas necesitas ffmpeg en el sistema (Windows:  winget install ffmpeg).

faster-whisper==1.2.1
sherpa-onnx==1.13.5

# Arrastrar y soltar archivos en la ventana (opcional; la app funciona sin el)
tkinterdnd2

# Aceleracion por GPU NVIDIA (CUDA). Comenta estas lineas si NO tienes GPU NVIDIA;
# el programa cae automaticamente a CPU.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43
nvidia-cuda-nvrtc-cu12==12.9.86
